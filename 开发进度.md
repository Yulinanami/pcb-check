# 开发进度

## 2026-05-16

### 已完成

- 完成 PCB 瑕疵检测最小流程代码：数据准备、训练、测试集评估、GT/预测可视化。
- 确认使用 GPU 环境训练：`torch 2.7.0+cu128`，CUDA 可用，显卡为 RTX 4060 Laptop GPU。
- 简化脚本调用方式，去掉命令行参数设计，统一直接运行：
  - `python scripts/prepare_dataset.py`
  - `python scripts/train_yolo.py`
  - `python scripts/evaluate_yolo.py`
  - `python scripts/visualize_detections.py`
- 训练脚本固定使用 `yolov8n.pt`、`imgsz=1024`、`epochs=50`、`batch=16`、`device="0"`、`cache=True`。
- 已完成一次训练，验证集结果约为：
  - `mAP50 = 0.935`
  - `mAP50-95 = 0.505`
- 发现测试集整图推理对小目标效果差，因此将评估和可视化改为滑窗推理：
  - 切片大小：`512`
  - 步长：`256`
  - 推理尺寸：`1024`
  - 合并方式：同类 NMS
- 当前完整测试集评估结果：
  - `mAP@0.5 = 0.032968`
  - `Mouse_bite AP = 0.001288`
  - `Open_circuit AP = 0.067265`
  - `Short AP = 0.013680`
  - `Spur AP = 0.063271`
  - `Spurious_copper AP = 0.019335`
- 已重新生成可视化结果到：`outputs/visualizations`。

### 当前主要问题

- 测试集 `Mouse_bite` 类 AP 很低，是当前主要短板。
- 模型在测试集上仍有较多误检，主要原因是 PCB 缺陷目标很小，且训练集与测试集图像尺寸、版型存在差异。

### 后续建议

- 如果继续提升测试集 mAP，优先考虑切片训练和小目标增强。
- 不建议继续只调整整图推理尺寸；提升有限且容易爆显存。

## 2026-05-17

### 继续提升记录

- 中断后的 `yolo26n` 长训练没有产出权重，已停止残留进程并清理失败目录。
- 没继续盲目重训，改为先做轻量推理后处理验证。
- 已验证并落地的提升：
  - 评估置信度：`0.001` -> `0.0005`
  - 滑窗合并 NMS：`0.5` -> `0.7`
  - 过滤最小边长小于 `8px` 的预测框
- 当前完整测试集结果：
  - `mAP@0.5 = 0.037082`
  - `Mouse_bite AP = 0.002048`
  - `Open_circuit AP = 0.079323`
  - `Short AP = 0.013867`
  - `Spur AP = 0.062730`
  - `Spurious_copper AP = 0.027442`
- 已重新生成可视化结果到：`outputs/visualizations`。

### 本轮继续优化

- 先尝试了 `512` 裁剪训练微调，测试集效果明显变差，未采用，并已清理失败数据集与权重目录。
- 改为在原训练集上从 baseline 权重继续 no-aug 微调：关闭 mosaic、scale、translate、HSV、水平翻转等强增强。
- 验证多段 no-aug 微调后，`pcb_yolo_noaug_ft4` 是当前最好单模型；后续 `ft5` 退步，未采用并已清理。
- 正式评估与可视化权重已切换为：`runs/train/pcb_yolo_noaug_ft4/weights/best.pt`。
- 后处理最小框边长从 `8px` 调整为 `10px`。
- 当前完整测试集结果：
  - `mAP@0.5 = 0.066002`
  - `Mouse_bite AP = 0.022109`
  - `Open_circuit AP = 0.124115`
  - `Short AP = 0.027522`
  - `Spur AP = 0.093705`
  - `Spurious_copper AP = 0.062556`
- 已重新生成可视化结果到：`outputs/visualizations`。
