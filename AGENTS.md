# PCB 瑕疵检测任务要求

本项目当前任务是基于提供的 PCB 板瑕疵训练集和测试集完成目标检测分析与实验。

## 数据与类别

- 训练集路径：`D:\myprojects\机器学习和深度学习\训练集-PCB_DATASET`
- 测试集路径：`D:\myprojects\机器学习和深度学习\PCB_瑕疵测试集`
- 固定 5 个缺陷类别，不可删改：
  - `0 Mouse_bite`
  - `1 Open_circuit`
  - `2 Short`
  - `3 Spur`
  - `4 Spurious_copper`

## 标注格式

- 训练集标注位于 `训练集-PCB_DATASET\Annotations`，格式为 VOC XML，框为绝对像素坐标。
- 训练集图片位于 `训练集-PCB_DATASET\images`。
- 测试集标注为 YOLO txt 格式，每行含 `class_num x y w h`。
- YOLO txt 中的 `x y w h` 分别表示归一化后的标注框中心点坐标、相对宽和相对高。

## 任务目标

1. 使用训练集训练或微调 PCB 瑕疵目标检测模型，例如 YOLO。
2. 在完整测试集上计算指标：
   - `mAP@IoU=0.5`
   - 每个类别的 `AP`
3. 说明提高 `mAP` 的方法。
4. 可视化展示测试图中的 GT 标注框和模型检测结果，并在同一张图上进行对比。

## 注意事项

- 不能只根据单张图片判断检测效果，必须对测试集整体评估。
- PCB 瑕疵目标很小，默认 YOLO 不改动时效果可能很低，预计 `mAP@0.5` 大约在 `0.005-0.01` 左右。
- 可视化时尽量不要让框和长文字遮挡大面积图像；可以使用短标签或颜色区分类别。
- 该任务是目标检测任务，不是图像分类任务，也不是语义分割任务。
- 后续如写代码，优先保持最小实现：数据格式转换、训练配置、评估指标、结果可视化。
